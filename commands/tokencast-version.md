---
description: Show the installed tokencast version
allowed-tools: Bash, Read
---

Run the following command and display the output:

```
bash "$CLAUDE_PROJECT_DIR/.claude/skills/tokencast/scripts/tokencast-learn.sh" --version
```
